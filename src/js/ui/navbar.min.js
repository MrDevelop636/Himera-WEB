function initNavbar(){let a=document.querySelector(".navbar");window.addEventListener("scroll",()=>{window.scrollY>50?a.classList.add("navbar--scrolled"):a.classList.remove("navbar--scrolled")})}export default initNavbar;